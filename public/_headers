# Cloudflare Workers 静态资源的响应头（这个文件本身不会被当页面发出去）。
# 资源带 ?v=时间戳，所以可以长缓存；HTML 仍然每次回源校验。
# 多条规则同时命中时头会叠加（同名头用逗号拼起来），所以先写 /* 通用规则，
# 再在具体路径里用「! Cache-Control」摘掉通用值后重设。
# CSP：唯一外部脚本是 Cloudflare 自动注入的 Insights beacon；style-src 保留 'unsafe-inline'
# 是因为 render.js 里还有 style="…" 内联属性。首页 JSON-LD 上线时（批次 2）要把它的 sha256 加进 script-src。

/*
  Cache-Control: public, max-age=0, must-revalidate
  Referrer-Policy: strict-origin-when-cross-origin
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Permissions-Policy: geolocation=(), camera=(), microphone=()
  Content-Security-Policy: default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self' https://static.cloudflareinsights.com; connect-src 'self' https://cloudflareinsights.com; base-uri 'none'; form-action 'none'; frame-ancestors 'none'; object-src 'none'

/assets/*
  ! Cache-Control
  Cache-Control: public, max-age=31536000, immutable

/og.png
  ! Cache-Control
  Cache-Control: public, max-age=604800
/favicon.svg
  ! Cache-Control
  Cache-Control: public, max-age=604800
/favicon.ico
  ! Cache-Control
  Cache-Control: public, max-age=604800
/apple-touch-icon.png
  ! Cache-Control
  Cache-Control: public, max-age=604800
